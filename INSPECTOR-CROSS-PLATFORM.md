# Inspector Agent - 跨平台全局监管系统

## 概述

Inspector Agent 是 PDCO 工作流中的 **L0 全局监管者**，负责对项目中所有 Agent（编程、分析、设计等）进行统一的质量评估、性能追踪、反馈指导和自动系统调整。

**关键特性**：
- 🌍 **跨平台统一标准**：Claude、Codex、Gemini、Cursor 使用相同的评估和奖惩机制
- 📊 **多 Agent 管理**：不仅仅编程，所有 Skills 下的 Agent 都被纳入管理
- 🔄 **三方同步**：Worker → Inspector → Human 通过 CLI 仪表盘实时协调
- 📈 **智能升降**：Token 预算和积分自动根据表现升降
- 🧠 **知识聚合**：个人 self.opt 聚合为 team self.opt

---

## 三层架构

```
┌────────────────────────────────────────────────────────┐
│  L0: Inspector Agent (全局监管者)                      │
│  • 所有平台统一标准                                    │
│  • 多 Agent 评估与对比                                 │
│  • 自动资源调度                                        │
│  • 质量门控                                            │
└────────────────────────────────────────────────────────┘
               ↓
┌────────────────────────────────────────────────────────┐
│  L1: Skill Agents (具体执行)                           │
│  • 编程: Frontend/Backend/Tester                       │
│  • 分析: Researcher/Analyst/Debugger                   │
│  • 设计: UX/Architect/Engineer                         │
│  • 其他: 所有 Skills 下的 Agent                        │
│  • 遵循: PDCO 工作流 + 个人 self.opt                   │
└────────────────────────────────────────────────────────┘
               ↓
┌────────────────────────────────────────────────────────┐
│  L2: 同步层 (三方信息流)                               │
│  • Worker Agents: 执行 → 日志                          │
│  • Inspector Agent: 评估 → 反馈                        │
│  • Human: 监督 → 决策                                  │
│  • CLI: 实时同步仪表盘                                 │
└────────────────────────────────────────────────────────┘
```

---

## 跨平台实现

### Claude Code

**位置**：`skills.claude/sam-dev-cc-init/.claude/agents/inspector-agent.md`

**特点**：
- ✅ Agent 内嵌式 (`.claude/agents/`)
- ✅ CLI 仪表盘 (`inspector-cli.sh`)
- ✅ 支持多 Agent 查询和对比
- ✅ 实时三方同步

**使用**：
```bash
# 查看 Agent 状态
.claude/scripts/inspector-cli.sh status frontend

# 查看全局仪表盘
.claude/scripts/inspector-cli.sh dashboard all

# 查看团队风险
.claude/scripts/inspector-cli.sh risks
```

---

### Codex

**位置**：`skills.codex/inspector/.codex/skills/inspector/SKILL.md`

**特点**：
- ✅ Skill 驱动式（自动触发）
- ✅ 支持多个评估命令 (`$inspector ...`)
- ✅ 场景化的反馈
- ✅ 与 Codex Agent 流程集成

**使用**：
```bash
# 评估单个任务
$inspector evaluate-task

# 检测问题模式
$inspector detect-pattern

# 生成周度报告
$inspector weekly-report

# 推荐资源分配
$inspector recommend-tasks
```

---

### Gemini CLI

**位置**：`skills.gemini/inspector/SKILL.md`

**特点**：
- ✅ 查询式（对话交互）
- ✅ 支持自然语言查询
- ✅ 适合探索性分析
- ✅ 与 Gemini 对话流程集成

**使用**：
```bash
# 评估 Agent
gemini "Evaluate Frontend Agent's latest task: A-grade, 8/8 CHECKFIX"

# 全局分析
gemini "Generate weekly team performance report"

# 模式识别
gemini "Identify common CHECKFIX issues across all Agents"

# 资源优化
gemini "Recommend task priority based on Agent performance"
```

---

### Cursor Rules (计划)

**位置**：`.cursor/rules/inspector-supervision.mdc`

**特点**：
- 在编辑器中显示 Agent 状态
- 自动提示质量检查项
- 集成反馈到编辑器通知

---

## 统一标准

所有平台使用**相同的**评估标准和奖惩机制：

### 质量等级

| 等级 | 标准 | 积分 | 预算调整 |
|------|------|------|---------|
| **A** | 一次通过、零改进 | +15 | 升级倒计时 -1 |
| **B** | 小修正 (<3 处) | +7 | 保持 |
| **C** | 返工（结构问题） | -20 | 降级 + 冷静期 |
| **D** | 废弃/完全重写 | -50 | 直接降级 |

### Token 预算等级

| 等级 | 上限 | 升级条件 |
|------|------|---------|
| 🔴 严格 | 3k | 降级后自动分配 |
| 🟡 标准 | 8k | 默认起始 |
| 🟢 宽松 | 15k | 连续 3 次 A 级 |
| 🔵 信任 | ∞ | 连续 5 次 A 级 |

### 反馈框架

所有平台生成相同格式的反馈：

```
[EVALUATION] Agent Performance: EXCELLENT
[ALERT] Pattern Detected: {type}
[CRITICAL] Task Delivery: REWORK REQUIRED
[CRITICAL ALERT] Quality Degradation
[FINAL] Ultimate Ultimatum
```

---

## CLI 仪表盘（跨平台同步）

Inspector Agent CLI 提供统一的三方同步界面：

```bash
# Claude 的 CLI 仪表盘
.claude/scripts/inspector-cli.sh

# 显示所有 Agent 状态
./inspector-cli.sh dashboard all

# 显示团队指标
./inspector-cli.sh teams

# 显示风险告警
./inspector-cli.sh risks

# 显示三方同步状态
./inspector-cli.sh sync all
```

**输出示例**：
```
┌─────────────────────────────────────────────────────────┐
│         PDCO Inspector Agent - Global Dashboard         │
└─────────────────────────────────────────────────────────┘

[AGENT STATUS SUMMARY]
├─ Frontend Agent: ✨ A | 145 pts | Generous (15k)
├─ Backend Agent:  👍 B | 87 pts  | Standard (8k)
├─ Analyst Agent:  ✨ A | 128 pts | Generous (15k)
├─ Designer Agent: 👍 B | 76 pts  | Standard (8k)
└─ Tester Agent:   ⚠️  B | 92 pts  | Standard (8k)

[TEAM METRICS]
├─ Avg Grade: A-
├─ Team Token Efficiency: 87%
├─ CHECKFIX Compliance: 97%
└─ Weekly Points: +89

[RISK ALERTS]
├─ Designer: Pattern detected - needs mentoring
└─ No critical alerts

[SYNCHRONIZATION]
├─ Worker Agents: All online
├─ Inspector: Last update 5 min ago
└─ Human: Dashboard refreshed
```

---

## 知识库聚合（Team Self.opt）

各 Agent 的个人 `self.opt` 自动聚合为 `team-self.opt/`：

```
team-self.opt/
├── patterns/
│   ├── common-errors.md        # 跨 Agent 常见错误
│   ├── anti-patterns.md         # 反面案例
│   └── success-patterns.md      # 成功案例（可复用）
│
├── metrics/
│   ├── efficiency-trends.md     # 团队效率趋势
│   ├── quality-distribution.md  # 质量分布
│   └── team-benchmarks.md       # 团队基准
│
└── recommendations/
    ├── resource-allocation.md   # 资源分配建议
    ├── training-needs.md        # 培训需求
    └── process-improvements.md  # 流程改进机会
```

---

## 使用指南

### 1. 初始化

在项目根目录运行 `sam-init` 初始化 PDCO 工作流：

```bash
.claude/commands/sam-init.md
```

这会创建：
- `CLAUDE.md` - 项目规范（含 Inspector 说明）
- `PROGRESS-LOG.md` - 进度日志
- `tasks/TASKS.md` - 任务管理
- `self.opt` - 个人经验库（Agent 维护）

---

### 2. 各平台查询

**Claude**：使用 CLI 仪表盘
```bash
.claude/scripts/inspector-cli.sh status frontend
.claude/scripts/inspector-cli.sh dashboard all
```

**Codex**：使用 Skill 命令
```bash
$inspector evaluate-task
$inspector weekly-report
```

**Gemini**：使用自然语言查询
```bash
gemini "Show team performance report"
gemini "Which agents need improvement?"
```

---

### 3. 反馈处理

所有平台的反馈流程相同：

1. **PLAN 阶段**：Inspector 生成启动检查清单
2. **DO 阶段**：可选的实时反馈
3. **CHECK 阶段**：验证 CHECKFIX 通过
4. **OPT 阶段**：生成完成评估和系统调整

---

## 自动化规则

### Token 预算自动升降

| 事件 | 触发条件 | 动作 |
|------|---------|------|
| 升级 | 连续 3 次 A 级 | 🟡 → 🟢 (8k → 15k) |
| 升级 | 连续 5 次 A 级 | 🟢 → 🔵 (15k → ∞) |
| 降级 | 1 次 C 级 | 任何 → 🟡 标准 (8k) |
| 降级 | 1 次 D 级 | 任何 → 🔴 严格 (3k) |
| 冷静期 | 降级后 | 3 次任务内不得申请升级 |

### 积分自动调整

| 行为 | 积分 |
|------|------|
| A 级交付 | +15 |
| B 级交付 | +7 |
| C 级返工 | -20 |
| D 级废弃 | -50 |
| 预估精准 | +5 |
| 预估严重偏离 | -10 |
| 跳过 CHECKFIX | -50 |
| CHECKFIX 一次通过 | +10 |

---

## 最佳实践

1. ✅ **保持一致**：所有 Agent 遵循相同标准
2. ✅ **及时反馈**：不在最后才指出问题
3. ✅ **透明决策**：所有调整都有明确的触发条件
4. ✅ **学习导向**：Focus 改进而非惩罚
5. ✅ **自动化优先**：标准决策自动化，异常人工审核
6. ✅ **跨平台同步**：使用 CLI 确保信息一致性

---

## 故障排查

### Q: 为什么我的 Agent 预算被降级了？

**A**：检查：
1. 最近的成绩等级（查看 PROGRESS-LOG.md）
2. CHECKFIX 合规（是否有失败项）
3. 是否有 C 或 D 级的任务

使用 `inspector-cli.sh` 查看完整评估历史。

### Q: 如何快速升级预算？

**A**：连续完成 A 级任务：
- 保证 CHECKFIX 8/8 全过
- 预估控制在 ±20% 范围内
- 一次性交付，零改进

### Q: 团队知识库在哪里？

**A**：位置：`team-self.opt/`
- 包含所有 Agent 的聚合经验
- 定期更新（每周从个人 self.opt 汇总）
- 新 Agent 入职可从这里学习

---

## 相关文档

- [CLAUDE.md](./skills.claude/sam-dev-cc-init/.claude/assets/CLAUDE.md.template) - Claude Code 规范
- [Inspector Agent](./skills.claude/sam-dev-cc-init/.claude/agents/inspector-agent.md) - 详细说明
- [Inspector CLI](./skills.claude/sam-dev-cc-init/.claude/scripts/inspector-cli.sh) - 仪表盘工具
- [Codex Inspector](./skills.codex/inspector/.codex/skills/inspector/SKILL.md) - Codex 版本
- [Gemini Inspector](./skills.gemini/inspector/SKILL.md) - Gemini 版本
